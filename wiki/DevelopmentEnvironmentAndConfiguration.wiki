#labels Featured,Phase-Requirements
Lists the necessary local environment and IDE settings for building and deploying _tll_ projects.

=Required Software=
The following software is required:
|| *Name* || *Version* || *Notes* ||
|| [http://java.sun.com/javase/downloads/index.jsp jdk] || 1.6.0_11+ || ||
|| [http://www.eclipse.org/ eclipse] || 3.4.2+|| *NOT the JEE version but the Java version!!* ||
|| [http://maven.apache.org/ maven] || 2.1.0+|| ||
|| [http://ant.apache.org/ ant] || 1.7.1+ || Not strictly necessary as maven maintains its own instance. ||
|| [http://groovy.codehaus.org/ groovy] || 1.6+ || Use the windows installer download is possible. ||
|| [http://subversion.tigris.org/ subversion client] || 1.6.0+ || Preferred windows client: [http://www.sliksvn.com/en/download silksvn] ||
|| [http://www.eclipse.org/aspectj/downloads.php AspectJ] || 1.6.3+ || ||
|| [http://code.google.com/p/google-web-toolkit/ GWT] || 1.6 RC2+ || ||

=environment=
Ensure the following environment variables exist:
|| *Name* || *Description* ||
|| JAVA_HOME || Points to the root Java install dir. ||
|| M2_HOME || Points to the maven install dir. ||
|| M2_REPO || Points to your local maven repository dir. ||
|| ASPECTJ_HOME || Points to the root AspectJ install dir. ||
|| GROOVY_HOME || Points to the root groovy install dir. ||
|| ANT_HOME || Points to the root Ant install dir. ||
|| GWT_HOME || Points to the root GWT _install_ dir. (Used for building GWT from source.) ||
|| GWT_TOOLS || Points to the root GWT Tools dir. (Used for building GWT from source.)||
|| TLL_ROOT || The root dir of the local working copy of the tll source tree ('http://tll.googlecode.com/svn/trunk'). ||
|| TLL_TOOLS || Points to the local working copy dir of tll tools root dir ('http://tll.googlecode.com/svn/tools'). ||

  * Add the following paths to the system path:
 {{{
{JAVA_HOME}/bin
{M2_HOME}/bin
{ASPECTJ_HOME/bin
{GROOVY_HOME}/bin
{ANT_HOME}/bin
{GWT_HOME}
}}}

  * Append these global ignores to the installed SVN config file:
 {{{
global-ignores = war debug.bat shell.bat .metadata build classes bin bin-groovy dist Desktop.ini *.log *.tmp target .metadata .gwt-tmp test-output tomcat Constants.properties config-local.properties
}}}

  * Establish local working copies of the root tll project dir and tll tools dir:
 {{{
svn checkout http://tll.googlecode.com/svn/trunk/ {TLL_ROOT}
svn checkout http://tll.googlecode.com/svn/tools/ {TLL_TOOLS}
}}}

  * Install the {{{ {TLL_ROOT}/build-tools}}} project into the local maven repository:
 {{{
mvn install
}}}

  * Install {{{ {TLL_ROOT} }}} project (including all tll child projects) into the local maven repository:
 {{{
mvn install
}}}

----
=eclipse=
The following steps cover the necessary [http://www.eclipse.org/ eclipse] IDE workspace settings.

==bootstrap==
Specify the -vm option in the eclipse.ini:
 {{{
-vm {JAVA_HOME}/bin/javaw.exe
}}}

==Classpath Variables==
The following _Preferences -> Java -> Build Path -> *Classpath Variables*_ are required:
|| TLL_TOOLS || Replica of the environment variable defined above. ||
|| TLL_ROOT || Replica of the environment variable defined above. ||
|| GWT_HOME || Points to the root install dir of the Google Web Toolkit. ||
|| GWT_INCUBATOR_HOME || Points to the root install dir of the [http://code.google.com/p/google-web-toolkit-incubator/ GWT Incubator] project. ||

==String Substitution==
The following _Preferences -> Run/Debug -> *String Substitutions*_ are required:
|| aspectj_home || Points to the root install dir of AspectJ. ||
|| gwt_host_webroot || {TLL_ROOT}/smbiz/smbiz-client/tomcat/webapps/ROOT ||

==Add Ons==
|| *Name* || *URL* || *Notes* ||
|| subclipse || [http://subclipse.tigris.org/update_1.6.x http://subclipse.tigris.org/update_1.6.x] || ||
|| m2eclipse || [http://m2eclipse.sonatype.org/update/ http://m2eclipse.sonatype.org/update/] || ||
|| TestNG || [http://beust.com/eclipse http://beust.com/eclipse] || ||
|| Groovy || [http://dist.groovy.codehaus.org/distributions/updateDev_1.6/ http://dist.groovy.codehaus.org/distributions/updateDev_1.6/] || ||
|| Aptana || [http://update.aptana.com/update/studio/3.4 http://update.aptana.com/update/studio/3.4] || Only select the HTML and CSS editor plugin options - we don't want the aptana dogma along for the ride - it's very large.||

==Preferences==
Import the tll eclipse preferences files.  These files are located under:  [http://tll.googlecode.com/svn/eclipse eclipse].