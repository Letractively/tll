<?xml version="1.0" encoding="UTF-8"?>
<assembly xmlns="http://maven.apache.org/plugins/maven-assembly-plugin/assembly/1.1.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/plugins/maven-assembly-plugin/assembly/1.1.0 
  http://maven.apache.org/xsd/assembly-1.1.0.xsd">

  <!--
    Aggregates all server-side dependencies into a target directory.
  -->

  <id>server-libs</id>
  <includeBaseDirectory>false</includeBaseDirectory>

  <!-- groupId:artifactId:type:classifier -->
  <dependencySets>

    <!-- unpack smbiz-persist deps -->
    <dependencySet>
      <scope>runtime</scope>
      <unpack>true</unpack>
      <outputDirectory>/</outputDirectory>
      <useProjectArtifact>false</useProjectArtifact>
      <useTransitiveDependencies>false</useTransitiveDependencies>
      <useTransitiveFiltering>false</useTransitiveFiltering>
      <includes>
        <include>com.tll:smbiz-persist:zip:${daoMode}-deps</include>
      </includes>
    </dependencySet>

    <!-- identify all deps not a part of smbiz-persist -->
    <dependencySet>
      <scope>runtime</scope>
      <outputDirectory>/</outputDirectory>
      <useProjectArtifact>false</useProjectArtifact>
      <useTransitiveDependencies>true</useTransitiveDependencies>
      <useTransitiveFiltering>true</useTransitiveFiltering>
      <!--
        Exclude smbiz-model and smbiz-persist deps since we have 
        already aggregated these deps by way of smbiz-persist.
      -->
      <excludes>
        <exclude>*smbiz-model*</exclude>
        <exclude>*smbiz-persist*</exclude>
        <exclude>*tll-model*</exclude>
        <exclude>*tll-dao*</exclude>
        <exclude>*sources*</exclude>
      </excludes>
    </dependencySet>

    <!-- get smbiz-model and smbiz-persist -->
    <dependencySet>
      <scope>runtime</scope>
      <unpack>false</unpack>
      <outputDirectory>/</outputDirectory>
      <useProjectArtifact>false</useProjectArtifact>
      <useTransitiveDependencies>false</useTransitiveDependencies>
      <useTransitiveFiltering>false</useTransitiveFiltering>
      <includes>
        <include>com.tll:smbiz-*:jar:*</include>
      </includes>
    </dependencySet>

  </dependencySets>

</assembly>
